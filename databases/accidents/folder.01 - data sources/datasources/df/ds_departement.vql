# REQUIRES-PROPERTIES-FILE - # Do not remove this comment!
CREATE OR REPLACE DATASOURCE DF ds_departement
    FOLDER = '/01 - data sources'
    ROUTE LOCAL 'LocalConnection' '${databases.accidents.folder.01 - data sources.datasources.df.ds_departement.ROUTE.LOCAL.PATH}'
    COLUMNDELIMITER = '\t'
    ENDOFLINEDELIMITER = '\n'
    HEADER = TRUE;

